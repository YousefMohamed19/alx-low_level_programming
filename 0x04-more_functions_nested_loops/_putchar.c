#include <unistd.h>
#include "main.h"

/**
 * _putchar.c write the character c to stdout
 * @c: the character to print 
 *
 * Return: on success 1.
 *         on error, -1 is returned, and error is set approprialty
 */
int _putchar.c(char c)
{
	return (write(1, &c, 1));
}
